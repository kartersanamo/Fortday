# To-do Execution Notes

This repository includes skeleton implementation for all plan to-dos:
- Ruleset + parity docs in `docs/`
- Modular plugin architecture in `modules/`
- Resource-pack pipeline in `resourcepack/` and `scripts/build_resource_pack.py`
- Initial domain services for building/editing/combat/HUD/map/lobby

Next iteration should deepen each subsystem from prototype to feature parity.
